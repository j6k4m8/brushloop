# Single source of truth for deploy domains/image tags used by docker-compose + Caddy.
WEB_DOMAIN=brushloop.jordan.matelsky.com
API_DOMAIN=api.brushloop.jordan.matelsky.com

# GHCR image settings consumed by deploy/up.sh.
# Optional overrides:
# - API_IMAGE (defaults from GHCR_NAMESPACE + BRUSHLOOP_IMAGE_TAG)
# - EDGE_IMAGE (defaults from GHCR_NAMESPACE + BRUSHLOOP_IMAGE_TAG)
GHCR_NAMESPACE=j6k4m8
BRUSHLOOP_IMAGE_TAG=main
