{
	# Set email for Let's Encrypt account if needed.
	# email you@example.com
}

{$WEB_DOMAIN} {
	encode zstd gzip
	root * /srv/flutter-web
	try_files {path} /index.html
	file_server
}

{$API_DOMAIN} {
	reverse_proxy api:8787
}
