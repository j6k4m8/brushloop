# BrushLoop Monorepo

This repository contains the BrushLoop MVP implementation:

- `app/`: Flutter client (iOS, Android, Web, macOS)
- `server/`: Node.js + TypeScript backend with SQLite and WebSockets
- `packages/shared/`: Shared TypeScript contracts and protocol schemas

See `TASKS.md` for active execution state and `docs/` for operational documentation.
