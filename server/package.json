{
  "name": "@brushloop/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "dev": "node --experimental-strip-types --watch src/index.ts",
    "lint": "node ../scripts/lint.mjs src test",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "build": "node ../scripts/noop-build.mjs server",
    "format": "node ../scripts/format-check.mjs src test"
  }
}
